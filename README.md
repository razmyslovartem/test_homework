# 🏦 Виджет банковских операций

Проект предназначен для обработки и анализа банковских транзакций клиента. Он предоставляет набор инструментов для фильтрации и сортировки операций, что помогает в автоматизации финансового учёта.

## ⚙️ Установка и настройка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone git@github.com:razmyslovartem/test_homework.git
    cd project
    ```

2.  **Убедитесь, что у вас установлены Python 3.8+ и Poetry.**
    Если Poetry не установлен:
    ```bash
    curl -sSL https://install.python-poetry.org | python3 -
    ```

3.  **Установите зависимости проекта:**
    ```bash
    poetry install
    ```

4.  **Активируйте виртуальное окружение:**
    ```bash
    poetry shell
    ```

## 🚀 Использование

Основная логика обработки данных находится в модуле `src.processing`.

### Импорт функций
```python
from src.processing import filter_by_state, sort_by_date
```

### 1. Фильтрация по статусу: `filter_by_state`
Функция возвращает список операций с указанным статусом (по умолчанию — `'EXECUTED'`).

**Сигнатура:**
```python
def filter_by_state(operations: list[dict], state: str = "EXECUTED") -> list[dict]:
```

**Пример использования:**
```python
operations = [
    {"id": 1, "state": "EXECUTED", "date": "2023-10-01"},
    {"id": 2, "state": "PENDING", "date": "2023-10-02"},
    {"id": 3, "state": "EXECUTED", "date": "2023-09-15"}
]

# Получить только выполненные операции
executed_ops = filter_by_state(operations, "EXECUTED")
print(executed_ops)
# Вывод: [{"id": 1, "state": "EXECUTED", ...}, {"id": 3, "state": "EXECUTED", ...}]

# Получить ожидающие операции
pending_ops = filter_by_state(operations, "PENDING")
print(pending_ops)
# Вывод: [{"id": 2, "state": "PENDING", ...}]
```

### 2. Сортировка по дате: `sort_by_date`
Функция сортирует список операций по дате (по умолчанию — по убыванию, от новых к старым).

**Сигнатура:**
```python
def sort_by_date(operations: list[dict], reverse: bool = True) -> list[dict]:
```

**Пример использования:**
```python
operations = [
    {"id": 1, "date": "2023-10-01T12:00:00"},
    {"id": 2, "date": "2023-09-01T15:30:00"},
    {"id": 3, "date": "2023-10-05T09:15:00"}
]

# Сортировка по убыванию (новые сверху)
sorted_desc = sort_by_date(operations, reverse=True)
print([op["id"] for op in sorted_desc])
# Вывод: [3, 1, 2]

# Сортировка по возрастанию (старые сверху)
sorted_asc = sort_by_date(operations, reverse=False)
print([op["id"] for op in sorted_asc])
# Вывод: [2, 1, 3]
```

### Комбинирование функций
```python
# Пример: получить выполненные операции, отсортированные от новых к старым
all_operations = [...] # Ваш список словарей с операциями
processed_data = sort_by_date(filter_by_state(all_operations), reverse=True)
```

## 🛠 Разработка

Проект поддерживает современные практики разработки на Python.

- **Структура проекта:** Код организован в соответствии с рекомендациями Python, основная логика находится в директории `src`.
- **Управление зависимостями:** Используется **Poetry** для воспроизводимых окружений. Все зависимости фиксируются в `poetry.lock`.
- **Стиль кода:** Код соответствует **PEP 8**. Рекомендуется использовать линтеры (например, `flake8`) и форматтеры (например, `black`).
- **Работа с Git:** Разработка ведётся по методологии **GitFlow**.
    - `main` — стабильная версия.
    - `develop` — ветка для интеграции новых функций.
    - Для каждой новой задачи создаётся отдельная ветка от `develop` с понятным именем (например, `feature/filter-by-state`).
- **Коммиты:** Делаются атомарные коммиты с понятными сообщениями на английском или русском языке.

### Запуск проекта для разработки
```bash
# Активировать окружение
poetry shell

# Установить pre-commit хуки (опционально)
poetry run pre-commit install
```


Этот файл готов к использованию. Вы можете:
1.  **Скопировать** весь текст выше.
2.  **Создать файл** `README.md` в корне вашего проекта в PyCharm.
3.  **Вставить** скопированный текст.
4.  **Сохранить** файл и **закоммитить** его в Git.
